# Loosened version pins so the project installs on current Python (incl. 3.14+).
# The previous exact pins (lxml==4.9.2, Levenshtein==0.20.9) no longer build on
# newer Python. The floors below are the original known-good versions; pip is free
# to pick anything newer that builds.
#
# If a newer release ever breaks something, fall back to the exact known-working
# snapshot instead:
#     pip install -r requirements-lock.txt
attrs>=22.2.0
fuzzysearch>=0.7.3
fuzzywuzzy>=0.18.0
Levenshtein>=0.20.9
lxml>=4.9.2
python-docx>=0.8.11
python-Levenshtein>=0.20.9
rapidfuzz>=2.13.7
